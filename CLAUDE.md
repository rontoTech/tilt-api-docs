# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The canonical guide is @AGENTS.md. Ground truth for everything documented here is the backend implementation (`bowstring-backend/src/trading-api.ts`, `auth-api.ts`, `agent-api.ts`) — verify against code before documenting. Money fields are decimal strings; `/v1/*` uses numeric error codes that must match `docs/errors.md`. The workspace-level guide (if present) is `../AGENTS.md`.
